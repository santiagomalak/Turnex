export default function EspaciosPage() {
  // Paso 2 del plan (PLAN_DESARROLLO.md): acá va el listado real de
  // espacios (canchas de fútbol, pádel, etc.) leído de Postgres
  // con `lib/db.ts`. Placeholder hasta correr la migración inicial.
  return (
    <main>
      <h1>Espacios</h1>
      <p>CRUD de espacios — pendiente (paso 2 del plan de desarrollo).</p>
    </main>
  );
}
