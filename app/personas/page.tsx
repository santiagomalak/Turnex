export default function PersonasPage() {
  // Paso 2 del plan (PLAN_DESARROLLO.md): acá va el listado real de
  // personas (socios, invitados, staff, profesores) leído de Postgres
  // con `lib/db.ts`. Placeholder hasta correr la migración inicial.
  return (
    <main>
      <h1>Personas</h1>
      <p>CRUD de personas — pendiente (paso 2 del plan de desarrollo).</p>
    </main>
  );
}
